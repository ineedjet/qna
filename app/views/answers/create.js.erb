$('.notice').html('<%= j render 'shared/notice' %>');
<% if @answer.errors.any? %>
  $('.new_answer .errors').html("<%= @answer.errors.full_messages.join('<br>') %>");
<% else %>
  $('textarea#answer_body').val('');
  $('.answers').append('<%= j render @answer %>');
  $('#new_answer .nested-fields').remove();
<% end %>